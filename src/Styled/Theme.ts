import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
    palette: {
      primary: {
        main: "#2980b9"
      },
      secondary: {
        main: "#3498db"
      },
    },
    typography: {
        fontFamily: 'Roboto, Arial',
      },
  });